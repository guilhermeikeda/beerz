import { StyleSheet, Text, View } from 'react-native';

/**
 * Text Styles
 */
export const TextStyle = StyleSheet.create({
  productTitle: {
    fontSize: 19,
  },

  title: {
    fontSize: 25,
    fontWeight: 'bold'
  }
});