import { StyleSheet } from 'react-native';

/**
 * Container Styles
 */
export const ContainerStyle =  StyleSheet.create({
  root: {
    borderRadius: 4,
    borderWidth: 0.5,
    borderColor: '#d6d7da',
  }
});